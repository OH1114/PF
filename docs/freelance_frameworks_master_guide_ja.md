# 副業案件を最短で取りにいくためのフレームワーク総合ガイド（2026版）

この資料は、**副業案件で実際に出やすい技術スタック**を、
- 利点/欠点
- どんな案件で採用されるか
- 学習優先度
- 最小デモの作り方

まで一気通貫で理解するための実践ガイドです。

---

## 1. 最新トレンド×副業案件で使われやすいフレームワーク一覧

| 区分 | フレームワーク | 強み（利点） | 注意点（欠点） | 副業案件で多い用途 | 案件単価目安（月） |
|---|---|---|---|---|---|
| フロント/フルスタック | Next.js (React) | SEO対応、SSR/SSG、App Router、Vercel連携が強い | 学習範囲が広い（RSC/Server Actions/キャッシュ） | LP+管理画面、SaaS MVP、CMS連携、ECフロント | 40〜100万円 |
| フロント | Nuxt (Vue) | Vueの学習コストが比較的低い、SSR容易 | React市場より案件母数がやや少ない | コーポレート、会員サイト、既存Vue案件改修 | 35〜80万円 |
| API/AI連携 | FastAPI | 実装速度、型ヒント、自動ドキュメント、AI/Python資産活用 | 大規模運用は設計力が必要 | AIチャット、業務自動化API、社内ツール連携 | 40〜90万円 |
| Web業務システム | Django | 認証/管理画面/ORMが強力、堅実 | モダンSPA構成は追加設計が必要 | 予約・受発注・在庫・会員管理 | 40〜85万円 |
| API/サーバサイドTS | NestJS | DI・構造化で保守しやすい、TypeScript統一 | 初学者は抽象度が高く感じやすい | BFF、LINE bot、社内API基盤 | 45〜100万円 |
| Web業務システム | Laravel | 日本案件母数が多い、情報量多い、CRUD高速 | TypeScript統一が難しい場合あり | EC、管理画面、業務システム改修 | 40〜90万円 |

> 単価目安は地域・稼働率・要件難度で大きく変動します。提案時は「工数見積もり + 保守範囲」を明記するのが重要です。

---

## 2. 案件タイプ別の「勝ち筋」

### A. LP + 管理画面（短納期）
- 推奨: Next.js / Nuxt
- 勝ち筋:
  - 最初にデザインシステム（色・タイポ・余白）固定
  - CMS or JSON管理で文言差し替えを簡単に
  - 納品時に「運用手順書」を付ける

### B. AI機能付き業務自動化
- 推奨: FastAPI + Streamlit/Next.js
- 勝ち筋:
  - AIが落ちても動くフォールバック
  - 料金事故を防ぐ上限設定
  - 入出力仕様（CSV/JSON/PDF）を先に合意

### C. 予約・顧客・在庫などの業務システム
- 推奨: Django / Laravel
- 勝ち筋:
  - 権限設計（管理者/担当/閲覧者）を初期設計
  - 監査ログとCSV出力を先に実装
  - 既存運用に合わせた画面遷移優先

### D. API基盤・Bot連携
- 推奨: NestJS / FastAPI
- 勝ち筋:
  - OpenAPI仕様を先に作る
  - エラーレスポンスを統一
  - Webhookの署名検証と再送制御を実装

---

## 3. 本リポジトリに追加した「模擬案件デモ」一覧

| ディレクトリ | 想定案件 | 主技術 | 学べること |
|---|---|---|---|
| `portfolios/04_nextjs_saas_admin_mock` | SaaS管理画面MVP | Next.js | 認証、管理画面、環境変数、API連携構成 |
| `portfolios/05_fastapi_crm_mock` | 顧客対応自動化API | FastAPI | REST API設計、バリデーション、.env管理 |
| `portfolios/06_django_booking_mock` | 予約管理システム | Django | モデル設計、管理画面、業務テーブル構成 |
| `portfolios/07_nestjs_linebot_mock` | LINE通知Bot/API | NestJS | DI構造、Webhook、DB層分離 |
| `portfolios/08_laravel_ec_mock` | 小規模EC管理バックオフィス | Laravel | MVC構成、ルーティング、DBマイグレーション |

---

## 4. `.env` が初めてでも迷わないための基礎

## 4.1 `.env` の役割
- APIキー・DB接続先などの**機密情報をコードから分離**する。
- 本番/開発で値を切り替える。
- Git管理しない（`.gitignore`対象）。

## 4.2 典型例
```env
# アプリ共通
APP_ENV=local
APP_DEBUG=true
PORT=8000

# DB
DATABASE_URL=postgresql://user:password@localhost:5432/appdb

# APIキー
OPENAI_API_KEY=sk-xxxx
LINE_CHANNEL_SECRET=xxxx
LINE_CHANNEL_ACCESS_TOKEN=xxxx
```

## 4.3 実運用ルール
1. `.env.example` に「キー名だけ」を置く（値はダミー）。
2. 実値は `.env` に書き、コミットしない。
3. READMEに「どのキーが必須か」を書く。
4. デプロイ先のSecret Managerにも同じキー名で登録する。

---

## 5. よくある課題と解消策（副業案件で頻出）

| 課題 | 失敗パターン | 解消の工夫 |
|---|---|---|
| 要件が曖昧 | 開発後に「思ってたのと違う」 | 画面遷移図 + API I/Oサンプルを先に合意 |
| 納期が短い | 作り込みすぎて遅延 | MVP範囲をMust/Should/Wantで分割 |
| API費用が読めない | 想定外課金 | キャッシュ、上限、フォールバック導入 |
| バグ再現しない | 感覚で修正 | ログID、再現手順、テストケース固定 |
| 引き継ぎで詰む | 属人化 | README・ER図・環境変数表を必ず残す |

---

## 6. 最短効率ロードマップ（8週間）

1. **Week 1-2**: 既存3ポートフォリオを動かし、READMEを読みながら構成を把握。
2. **Week 3-4**: 追加した模擬案件5つのディレクトリを読み、各フレームワークの責務分離を比較。
3. **Week 5-6**: 1つ選んで本当に動く最小実装に拡張（ログイン・CRUD・テスト）。
4. **Week 7**: 仕様書テンプレ（要件/画面/API/テスト）を自作。
5. **Week 8**: 提案文テンプレ + 見積テンプレ + デモ動画を作成。

---

## 7. 提案時にそのまま使える説明テンプレ

> 本実装では、短納期案件に対応するため、
> 1) フレームワーク標準構成を採用して保守性を担保し、
> 2) `.env` による機密分離で運用安全性を確保し、
> 3) 機能をMVPに限定して早期リリースを優先しました。
> 次フェーズで、監査ログ・通知・分析基盤を段階的に追加可能です。

